name: Actualización Diaria de PRstats

on:
  schedule:
    - cron: "0 0 * * *"  # Todos los días a medianoche UTC
  workflow_dispatch:  # Permite ejecución manual desde GitHub

jobs:
  update_stats:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar el repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Dependencias
        run: |
          pip install -r requirements.txt

      - name: Ejecutar Script de Estadísticas
        run: |
          python PRstats_multiclans.py

      - name: 📤 Subir Cambios al Repositorio
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"
          git add -A
          git commit -m "Actualización diaria de estadísticas PR" || echo "Sin cambios para realizar commit."
          git push
