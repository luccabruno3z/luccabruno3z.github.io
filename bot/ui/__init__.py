"""Shared presentation layer (the bot's "design system").

Components V2 card builders so every command renders a consistent visual
language: tier-colored Container borders, Sections with the clan logo as a
Thumbnail accessory, native unicode bars instead of slow matplotlib PNGs, and
Separators for structure. Requires discord.py >= 2.6 (LayoutView).
"""
